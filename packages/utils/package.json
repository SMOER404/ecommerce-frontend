{
  "name": "@poizonmarket/utils",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "cross-env NODE_ENV=production npx tsup",
    "dev": "cross-env NODE_ENV=development npx tsup --watch",
    "lint": "eslint src/",
    "clean": "rm -rf .turbo node_modules dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "tsup": "^8.0.2"
  },
  "dependencies": {
    "clsx": "^2.1.0",
    "tailwind-merge": "^2.2.1"
  }
}
